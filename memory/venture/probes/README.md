# Live Probes

Keep one live `Probe` per file.

Each probe should pre-register:

- `assumption_id`
- `probe_type`
- `artifact_class`
- `target_evidence_class`
- `minimum_evidence_quality`
- `success_rule`
- `falsification_rule`
